CSRF:-

Laravel provides protection with csrf attracks by generating the csrf token. This is nothing but a raandom string that
is generated by the laravel application to verify the user request. If csrf token field is not included, the application
request will be rejected.

Redirect:- Redirect method is used to navigate from url to another url.

View():- Method is used to return the view of another url.

Routing Parameters:- There are two types of parameters. 1. Required Parameters 2. Optional Parameters

Middleware:-

Middleware acts as a layer b/w user and request

Request:- Path, is, url

Retrive the input (i). Input method (ii). Properties of request instance

Namespace
